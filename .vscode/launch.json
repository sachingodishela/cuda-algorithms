{
    "configurations": [
        {
            "name": "CUDA C++: Launch",
            "type": "cuda-gdb",
            "request": "launch",
            "preLaunchTask": "Build Current File With CUDA",
            "program": "${fileDirname}/${fileBasenameNoExtension}.elf"
        }
        
    ]
}   